#include <iostream>
#include "header.h"
using namespace std;

int main()
{
    START = NULL;
    struct node **q;
    Insbeg(100);
    Insbeg(200);
    Insbeg(300);
    Insbeg(400);
    //Insafter(q,700);
    traverse();
}
